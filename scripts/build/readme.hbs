<h1 align="center">{{package.name}}</h1>

{{package.description}}

<!-- extended_desc -->{{extended_desc}}<!-- /extended_desc -->

## Usage

{{#if (includes package.keywords "cli")}}
{{~#if (includes package.keywords "lib")}}
You can use this package either as CLI tool or as library.

To use as CLI tool:

{{/if}}
1. Install latest **NodeJS** for [Windows](https://nodejs.org/en/download/current/) or [Unix](https://nodejs.org/en/download/package-manager/)

2. Open console, navigate to your Minecraft directory (one with the `logs/` directory or `crafttweaker.log` file)
   ```sh
   > cd C:/Instances/MyModpack
   ```

3. Run:
    ```sh
    > npx {{package.name}} --help
    ```
{{/if}}

{{#if helpOutput}}
### Options

```shell
{{helpOutput}}
```
{{/if}}

{{~#if (includes package.keywords "lib")}}

## API
{{~#if (includes package.keywords "cli")}}

To use as library:
{{/if}}

1. Install package
  > ```shell
  > npm i {{package.name}}
  > ```

2. Import functions from package.
  > ```ts
  > import {...} from "{{package.name}}"
  > ```

{{typedoc}}

{{~/if}}

## Author

* https://github.com/Krutoy242

## Other tools

{{#each packages}}{{#if (includes package.keywords "cli")}}
* [{{package.name}}]({{package.homepage}}/tree/master/packages/{{name}}) - {{package.description}}
{{~/if}}{{/each}}
